To build and run this program:
1. Open a terminal or the terminal in Visual Studio Code, navigate to the project directory OS/assignment, and run: 
   make
2. After compilation, execute: 
   ./run <input_filename>.txt
   (Replace <input_filename>.txt with the name of your input file)